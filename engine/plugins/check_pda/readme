# =========================================================================== #
# NG CMS // РџР»Р°РіРёРЅС‹ // check_pda					                          #
# =========================================================================== #

РџР»Р°РіРёРЅ РїРѕР·РІРѕР»СЏРµС‚ РґРµР»Р°С‚СЊ РїСЂРѕРІРµСЂРєСѓ РЅР° mobile/tablet РІРЅСѓС‚СЂРё TWIG С€Р°Р±Р»РѕРЅРѕРІ:

РџСЂРёРјРµСЂС‹ РёСЃРїРѕР»СЊР·РѕРІР°РЅРёСЏ:
{% extends is_mobile() ? "layout_mobile.html.twig" : "layout.html.twig" %}
{% if is_mobile() %} ... {% endif %}
{% if is_tablet() %} ... {% endif %}
{% if is_mobile() and is_samsung() %} ... {% endif %}
{{ get_available_devices()|join("<br />")|raw }}